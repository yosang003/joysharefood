import React, { Component } from 'react';
import styles from './index.css';

class Classifly extends Component{
    render(){

        const { list } = this.props;

        return(
            <div></div>
        )
    }
}